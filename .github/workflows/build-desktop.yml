name: Build Bible Fighter

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: bible-fighter-${{ github.ref }}
  cancel-in-progress: true

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install
      - name: Repair and validate source
        run: npm run validate
      - name: Build Windows portable executable
        run: npx electron-builder --win portable --publish never
      - name: Verify Windows artifact
        shell: pwsh
        run: |
          $files = @(Get-ChildItem -Path dist -Filter *.exe)
          if ($files.Count -lt 1) {
            Write-Error 'No Windows .exe was produced.'
          }
          foreach ($file in $files) {
            if ($file.Length -lt 10000000) {
              Write-Error "Windows artifact is unexpectedly small: $($file.Name) ($($file.Length) bytes)"
            }
            Write-Host "Verified $($file.FullName) ($($file.Length) bytes)"
          }
      - name: Smoke test packaged executable
        shell: pwsh
        run: |
          $exe = @(Get-ChildItem -Path dist -Filter *.exe)[0]
          $env:BIBLE_FIGHTER_SMOKE = '1'
          $process = Start-Process -FilePath $exe.FullName -PassThru
          if (-not $process.WaitForExit(60000)) {
            Stop-Process -Id $process.Id -Force
            Write-Error 'Packaged Bible Fighter did not exit from smoke test within 60 seconds.'
          }
          if ($process.ExitCode -ne 0) {
            Write-Error "Packaged Bible Fighter smoke test failed with exit code $($process.ExitCode)."
          }
          Write-Host 'Packaged Bible Fighter smoke test passed.'
      - uses: actions/upload-artifact@v4
        with:
          name: bible-fighter-windows-x64
          path: dist/*.exe
          if-no-files-found: error
      - name: Publish Windows executable to GitHub Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "build-${{ github.run_number }}"
          $existing = gh release view $tag 2>$null
          if ($LASTEXITCODE -eq 0) {
            gh release upload $tag dist/*.exe --clobber
          } else {
            gh release create $tag dist/*.exe --title "Bible Fighter $tag" --notes "Windows x64 portable build. Download the .exe, extract if needed, and double-click to run."
          }

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install
      - name: Build macOS package
        run: npx electron-builder --mac --publish never
      - uses: actions/upload-artifact@v4
        with:
          name: bible-fighter-macos
          path: |
            dist/*.dmg
            dist/*.zip
          if-no-files-found: error
