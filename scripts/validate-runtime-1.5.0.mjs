import { existsSync, readFileSync } from 'node:fs';
import { resolve } from 'node:path';
const root=process.cwd(); const fail=m=>{console.error(`1.5.1 runtime validation failed: ${m}`);process.exit(1)};
const html=readFileSync(resolve(root,'playtest.html'),'utf8');
const scripts=[...html.matchAll(/<script\s+src=["']([^"']+)["']/g)].map(m=>m[1]);
for(const s of ['game7.js','game46.js','game47.js','game48.js']) if(!scripts.includes(s)) fail(`missing ${s}`);
if(!(scripts.indexOf('game46.js')>scripts.indexOf('game45.js')&&scripts.indexOf('game47.js')>scripts.indexOf('game46.js')&&scripts.indexOf('game48.js')>scripts.indexOf('game47.js'))) fail('combat runtime order invalid');
const preload=readFileSync(resolve(root,'electron/preload-runtime.cjs'),'utf8'); for(const m of ['1.5.1','data/combat-core.js','game46.js','game47.js','game48.js','game49.js']) if(!preload.includes(m)) fail(`preload missing ${m}`);
const pkg=JSON.parse(readFileSync(resolve(root,'package.json'),'utf8')); if(pkg.version!=='1.5.1') fail(`package drift ${pkg.version}`);
for(const f of ['game46.js','game47.js','game48.js','game49.js']) if(!pkg.build.files.includes(f)) fail(`package omits ${f}`);
if(!existsSync(resolve(root,'data/combat-core.js'))) fail('combat core data missing');
const slice=readFileSync(resolve(root,'game47.js'),'utf8'); for(const m of ['BIBLE_FIGHTER_VERTICAL_SLICE_READY','drawValley','drawSea','drawDavid','drawMoses','drawSkillFx']) if(!slice.includes(m)) fail(`vertical slice missing ${m}`);
const polish=readFileSync(resolve(root,'game48.js'),'utf8'); for(const m of ['BIBLE_FIGHTER_RUNTIME_POLISH_READY','frameMs','fps','prunedFx','prunedShots']) if(!polish.includes(m)) fail(`runtime polish missing ${m}`);
const core=readFileSync(resolve(root,'data/combat-core.js'),'utf8'); for(const m of ['BIBLE_COMBAT_CORE','startup','active','recovery','normal5']) if(!core.includes(m)) fail(`combat core data missing ${m}`);
const diagnostics=readFileSync(resolve(root,'game49.js'),'utf8'); for(const m of ['BIBLE_FIGHTER_COMBAT_CORE_READY','diagnostics','snapshot','transitions','invalidStates']) if(!diagnostics.includes(m)) fail(`combat diagnostics missing ${m}`);
console.log(`1.5.1 runtime validation passed: ${scripts.length} scripts + complete vertical slice + runtime stability/performance polish + combat core foundation`);
